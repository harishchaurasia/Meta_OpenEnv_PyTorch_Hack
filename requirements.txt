# Core (always needed)
numpy
pydantic

# Local demo
matplotlib
streamlit

# OpenEnv deployment (uncomment or install separately for server / HF Spaces)
# openenv-core[core]>=0.2.1
# uvicorn[standard]
# fastapi

# TRL training skeleton (uncomment for Colab / GPU training)
# trl
# transformers
# datasets
# accelerate
